# Starter dependencies for the assignment.
#
# Install with:
#     python -m venv .venv
#     .venv\Scripts\activate        (Windows)
#     source .venv/bin/activate     (macOS / Linux)
#     pip install -r requirements.txt
#
# Versions are pinned loosely so this keeps working. When you submit, replace
# this file with the output of `pip freeze` so we can reproduce your exact
# environment.

pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
jupyter>=1.0
